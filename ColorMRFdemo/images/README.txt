This directory contains noisy synthetic images. Each image is stored in PPM
as well as in BMP format.

color.ppm (resp. color.bmp) is the original image. Noisy images
(having a "color_" prefix) are produced by adding zero mean Gaussian
noise with various covariances.
